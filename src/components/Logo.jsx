export default function Logo({selectedGroup}){
    // const [firstLetter, secondLetter] = selectedGroup.split('');
    return <>
           <span className="w-8 h-8 bg-blue-500 rounded-full flex justify-center items-center mr-1">
           {/* {firstLetter.toUpperCase()}{secondLetter.toUpperCase()} */}
      </span>
    </>
}